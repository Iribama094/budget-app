# Expo public env vars (available at runtime)
# Point this to the BASE of your backend (no trailing /v1)
# Examples:
#   Deployed (Vercel): https://your-deployment.vercel.app
#   Vercel dev (Android emulator): http://10.0.2.2:3000
#   Vercel dev (iOS simulator): http://localhost:3000
#   Vercel dev (physical device): http://<YOUR_LAN_IP>:3000
EXPO_PUBLIC_API_BASE_URL=

# Live bank connections (Mono Connect). Public key from https://app.withmono.com; the secret key goes on the server.
EXPO_PUBLIC_MONO_PUBLIC_KEY=
