# Optional. The app is preconfigured for the BudgetFriendly Supabase project (see src/config.ts),
# so no .env is needed. Set these only to point the app at a different Supabase project.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Live bank connections (Mono Connect). Public key from https://app.withmono.com; the secret key goes on the server.
EXPO_PUBLIC_MONO_PUBLIC_KEY=test_pk_uwxs2htr1ww5vseclevh
